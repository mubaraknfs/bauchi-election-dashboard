const SUMMARY_API =
  `${API_URL}/api/dashboard-summary`;

const PARTY_API =
  `${API_URL}/api/party-results`;

const LGA_API =
  `${API_URL}/api/lga-live-results`;

const TICKER_API =
  `${API_URL}/api/live-ticker`;